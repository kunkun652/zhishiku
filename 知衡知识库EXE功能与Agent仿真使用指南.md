# 知衡知识库 EXE：功能说明与仿真 Agent 使用指南

更新日期：2026-09-21。对应“成品1”源码提交 `91786c417ad952bb35417ddb7314a219b161d43c`，程序标识 `1.6.0-trusted-workbench`。

本文按当前源码、界面入口和交付记录编写，区分已经实现的能力与需要外部 Agent、模型服务或人工完成的工作。数据数量、后台进度、模型连接和工具可用性以本机实际状态为准，不是版本固有保证。

## 1. 先理解这个软件的定位

知衡是仿真工作的“资料库、依据库和任务准备台”，不是一个内置全部求解器的自动仿真软件。

对人：把原始文件、材料参数、工况、模型、案例和方法分开管理，帮助找资料、读原文、理解结论依据、检查缺口。

对 Agent：提供可检索、可定位、带版本的知识，让 Agent 在外部仿真环境中更有依据地选择方法、调用工具、检查结果和记录失败。真正的几何操作、网格生成、求解与后处理，仍由外部 CAE 工具和 Agent 执行。

必须区分：**检索相关 ≠ 工程适用；文件已入库 ≠ 内容已复核；工具运行成功 ≠ 结果正确；自动检查通过 ≠ 人工审核或批准。**

### 启动与版本

- 本机日常入口：`D:\zhishiku\新版知衡知识库.lnk`。
- 正式程序位置：`D:\zhishiku\desktop-framework\release\知衡仿真知识库\知衡仿真知识库.exe`。
- Git 分支保存源码；已打包 EXE 不会因为切换 Git 分支而自动改变。更新源码后，需要重新构建并部署才会改变 EXE。
- GitHub 提供源码和文档，不附带本机业务库、原始资料、模型权重和 EXE；克隆仓库不会自动得到本机所有知识。

## 2. 所有模块如何分工

| 界面模块 | 放什么 / 做什么 | 对人的帮助 | 对仿真 Agent 的帮助 |
| --- | --- | --- | --- |
| 工作台 | 检索、带引用问答、模型设置、后台概况 | 快速获得有出处的回答 | 获取任务相关证据，而非凭空生成参数 |
| 原始资料 | document 记录关联的原文件及分类 | 按文件找资料、读完整原文 | 找到知识源头，验证上下文和版本 |
| 知识卡片 | 材料、术语、网格策略、工况 | 阅读结构化知识及适用边界 | 将知识转换为受约束的任务输入 |
| 模型资产 | CAD、网格、求解输入、结果文件的登记与预览 | 辨认模型和版本 | 确认使用哪个模型、坐标系和区域映射 |
| 仿真案例 | 完整案例、失败经验 | 学习过程、避开已有错误 | 参考步骤、诊断原因，但不照搬条件 |
| 知识图谱 | 已登记对象与关系、二维/三维浏览 | 看清来源、引用和关联对象 | 沿关联补齐上下文、查找依赖 |
| 流程与能力 | 流程规则、Skill、Tool、验证基准 | 看懂怎么做、用什么、怎么检查 | 为规划、执行前检查和验证提供契约 |
| 运行与验证 | 任务、运行记录、缺口、训练/评测登记 | 看清任务要求、过程证据和未解决问题 | 保留真实执行轨迹，避免重复无效尝试 |
| 高级查看 → 原文片段与索引 | 正文片段、编码单元、索引与质量标记 | 排查漏检、错序、噪声和索引状态 | 定位检索故障、恢复处理断点 |
| 高级查看 → 任务知识包 | 选定对象、版本、文件校验值、关系和缺口的快照 | 整理某一次任务的依据 | 获取固定上下文，避免任务期间知识悄悄变化 |

日常通常只需要“工作台”和“原始资料”；其余模块用于查看结构化成果或准备具体仿真任务。上传、解析、抽取和更新索引属于后台处理，不要求使用者每天操作一套导入模板。

## 3. 工作台：检索和带依据的问答

### 3.1 只检索与检索并回答

“只检索”先查本地知识，显示相关对象和依据，适合直接找资料、核对有没有收录。它不要求配置问答模型。

“检索并回答”在检索之后调用已配置的 LLM，依据命中的原文形成回答，并做结论与引文的支持性核对。没有足够有效证据时，应保留缺口，而不是补出一个看似完整的工程答案。

操作建议：

1. 输入具体问题，尽量写明材料牌号、分析类型、模型条件和希望得到的内容。
2. 首先点击“只检索”，检查是否找到了正确资料。
3. 模型配置正确且资料使用范围允许后，再点击“检索并回答”。
4. 展开证据、打开原文，核对单位、前提、版本及上下文。
5. 对将进入求解输入的参数，另做工程适用性确认。

示例问题：“请查找某牌号材料在室温下的弹性参数及原文来源；缺少的参数请明确列出。”这比“帮我把材料都配好”更容易核查。

### 3.2 当前检索到底是什么方式

工作台使用本地混合检索：关键词全文检索、BGE-M3 语义向量检索，以及有边界的图谱关联扩展，再融合候选结果。

- 关键词检索适合牌号、编号、专业术语等明确词语。
- 向量检索帮助找到含义接近但措辞不同的内容；相似度不是正确率或适用性评分。
- 图谱扩展补充有关联的知识，不能把“有连接”当作因果证明。
- Semantica 派生图谱须满足版本一致性条件；不可用或不一致时，不能假装已经使用最新派生图谱。
- 工作台当前请求最多 8 个候选对象；对象数不等于原文引文条数。证据还受长度预算和来源校验限制。
- 某个检索组件不可用时可能降级使用可用路径。因此有结果并不证明所有组件都正常。

这不是联网搜索，也不是 LLM 自动进行多轮检索规划。当前生产检索不通过 Jev 分类或重排。接口支持的精确筛选条件，也不能理解为模型会从任意自然语言问题中自动提取并强制应用。

### 3.3 结论级引用有什么用

回答把具体结论绑定到证据编号、引文、原文位置和文件版本，而不是在整段答案末尾笼统列几个文件名。

程序会核对来源与引文，模型还会做一次支持性判断；未得到支持的结论不展示为有效结论。生成过程中来源变化，相关回答会被拒绝。历史回答的来源改变后，应显示失效状态，而不是静默套用新原文。

对人，这是复核入口；对 Agent，这是追踪每个判断依赖了什么的基础。但模型核对仍可能出错，不能替代人的工程判断。

### 3.4 模型设置与思考参数

在“模型设置”中填写：

| 配置 | 用途 | 注意事项 |
| --- | --- | --- |
| API URL | 兼容 Chat Completions 的服务地址 | 通常到 `/v1`，也接受完整 `/chat/completions` 地址 |
| API Key | 认证模型服务 | 不等于允许资料外发；不要写入源码或 GitHub |
| Model | 服务支持的实际模型标识 | 不能凭显示名称猜测；以服务商或本地服务为准 |
| 思考模式 | 控制兼容模型的推理参数 | 默认不额外传参；关闭、低、中、高等须由模型支持 |
| 参数格式 | 适配不同服务的思考字段 | 如 `reasoning_effort`、`enable_thinking`、`thinking.type`，不能通用混用 |
| 允许远端问答 | 打开远端服务使用的全局许可 | 还需要命中资料当前版本的逐文件许可 |

“保存并测试连接”使用简单测试语句检查连接，不发送业务全文，也不证明专业问答质量合格。实际回答是否可用仍要用真实问题核查。

Key 在本机使用当前 Windows 账户的 DPAPI 加密保存，界面不回显。留空保持原 Key，勾选清除才删除。换 Windows 账户后可能需要重新填写。

Embedding 与问答模型分开：当前复用本地 BGE-M3，不需要另外填写远端 Embedding Key。更换问答模型不要求重建向量索引；本地向量运行环境和模型文件仍需存在。

### 3.5 安静的后台状态

工作台用简洁状态提示后台是否处理中、等待模型、需要处理异常或等待索引更新。它用于知道是否需要介入，而不是要求用户手动管理每个流程节点。

“暂无待处理任务”只说明当前队列为空，不表示整库全部解析、全部编码、全部复核。“索引就绪”也不等于已切换为当前使用的索引。

## 4. 原始资料：只管理原始文件

这个模块只列资料记录关联的原文件，按库内现有分类浏览，不把材料卡、模型资产等全部混在文件列表中。

主要能力：按文件名、标题、标签和已解析全文查找；按分类缩小范围；打开右侧原文阅读；打开原文件；确认资料使用范围。

### 原文侧栏与阅读位置

- 点击文件名打开原文侧栏，阅读已解析的正文，切换页码及同页片段组。
- PDF 可以展开原件对照。未解析或不支持的文件，需要通过原件入口阅读；不是所有格式都能内嵌预览。
- 当前文件版本、页码和阅读滚动位置保存在本地浏览器存储；回到资料页可保留搜索与分类状态。
- 文件版本变化时不能直接沿用旧位置。清除浏览器存储或更换使用环境，也可能丢失阅读位置。
- 原文片段缺少可靠顺序时会提示待确认，不能把数据库 ID 当成原文页序。

对人：不用为了核对一句话反复寻找整个文件。对 Agent：能把抽取结果还原到来源，发现截断、漏页或上下文不适用。

扫描件或提取失败的文件可能没有可检索正文。看到文件已登记，不能推断已经完成 OCR 或全文索引。

### 资料外发规则

原文侧栏中的“资料使用范围”控制当前文件版本是否允许用于远端问答。默认保留在本地。

远端问答同时需要全局许可和所有命中文件当前版本的许可；只要有一份未授权，整次远端请求就不发送。文件内容变化后需要重新确认；更换模型 URL 会取消全局远端许可。

问答仅发送问题和必要引文等证据，不发送整库、本地文件路径或无关元数据。这仍属于资料外发，必须遵循资料自身许可、保密要求和服务商规则。授权不应仅因为“接口能连接”而开启。

## 5. 知识卡片：四种不同用途的结构化知识

四类在同一组中展示，点击类别查看已保存记录。卡片不是原文件的替代品；来源、单位、适用范围和限制应保留。

| 卡片 | 具体内容 | 对人的帮助 | 对 Agent 的帮助与限制 |
| --- | --- | --- | --- |
| 材料卡 | 牌号、状态、温度、方向、参数、单位、来源、MAT1/MAT8 等材料描述 | 快速核对材料本体参数 | 提供材料输入依据；不能把厚度、截面、铺层混入材料本体，也不能自动补齐未知参数 |
| 术语卡 | 定义、中英文别名、量纲、易混概念 | 统一理解，减少不同叫法带来的漏检 | 帮助理解问题和术语边界，不代替具体模型条件 |
| 网格策略卡 | 适用几何、单元类型、划分方法、质量指标、收敛要求 | 了解为什么采用某种网格 | 帮助选择网格方案和检查项；不能把历史网格尺寸当成通用正确值 |
| 工况卡 | core、conditional、upstream、mesh、material 层级，载荷、约束和作用区域 | 分清核心条件及对上游数据、网格、材料的依赖 | 将载荷和边界条件绑定当前模型；不能将其他案例的面编号或载荷直接复制使用 |

选择卡片时，至少核对分析类型、单位、材料状态、几何与网格版本、来源和限制。缺少任何关键前提，应登记“待确认”，而不是因为卡片存在就视为可执行输入。

## 6. 模型资产：管理模型，而不是自动证明模型正确

登记 CAD、网格、求解输入及结果的文件格式、坐标系、几何版本、网格版本、语义区域映射，以及厚度、截面、铺层等模型属性依据。

可以查看完整记录、附加原件、查看来源与版本，并预览受支持的文件。当前包含 STEP/STP、IGES/IGS、BREP、BDF/NAS/DAT、STL、OBJ 等预览入口，具体效果取决于文件内容和本机解析运行时。OP2 等文件可以登记原件，但不能据此宣称已经实现结果场完整解析。

对人：确认拿到的是哪个模型、外形是否符合预期、文件及版本是否对应。

对 Agent：知道后续工具应读取哪个资产，以及坐标系、区域映射和属性绑定有哪些依据。几何或网格改变后，旧的区域映射必须重新核查。

三维图形显示正常，不代表模型单位、网格质量、材料赋值、边界条件或求解结果正确。仿真计算仍在外部工具中执行。

## 7. 仿真案例与失败经验

仿真案例记录目标、假设、模型和卡片引用、步骤与工具版本、输入、日志、结果、可视化证据、独立检查，以及可复用部分与差异。

对人：不只看到“成功结果”，还能理解如何得到结果、哪些条件不能迁移。

对 Agent：用于参考执行顺序、选择检查项、定位常见故障，并比较当前任务与历史任务的差异。案例是参考依据，不是直接复制全部参数的模板，也不会在点击案例后自动重跑求解。

失败经验单独记录错误现象、诊断证据、可能原因、适用修复、失败尝试和停止条件。其价值是让人和 Agent 避免重复失败；未经证据确认的原因仍应保留为候选。

库内案例数量是登记记录数，不等于经过工程验收的成功任务数。

## 8. 知识图谱：看清关系和引用链

二维图谱支持拖动节点、拖动空白平移、滚轮缩放、点击查看和双击打开对象。设置中可以启用 Galaxy View 三维图谱、自动旋转、星空背景、适应窗口和恢复默认设置；三维支持旋转、平移和缩放。

选择对象后，可以查看类型、版本、状态、字段、原文件、来源、相邻对象和关系依据，也可以跳到原文。

常见关系包括“来源于、使用模型、采用材料、采用工况、采用网格策略、执行流程、调用工具、验收依据、产生结果、存在缺口、版本替代”。

对人：从模型找到使用的卡片，从结论找到资料，从失败找到运行记录。

对 Agent：沿已登记关系补齐任务上下文，发现缺失依赖和过期关联。关系端点版本变化后，旧关系需要复核；图形连线不等于工程因果，也不保证适用性。

### 对象详情中的引用关系

“已有关系与引用”区分出链、反向引用和候选建议。可以打开关联对象、建立关系，或查找本地显式引用建议，并接受、拒绝或撤销候选。

当前显式引用发现检查明确 ID 及有限正文范围，不是理解所有文件语义的无限自动建图。否定语句、条件和歧义仍需核对。“接受登记”仅表示登记关系，不是工程批准。

## 9. 流程与能力：怎么做、用什么、如何检查

| 类别 | 具体作用 | 对人的帮助 | 对 Agent 的帮助 |
| --- | --- | --- | --- |
| 流程规则 | 定义目标、有序步骤、依赖、输入输出、分支和停止条件 | 理解任务顺序和交付要求 | 规划步骤，知道缺条件时停止，而非跳过检查 |
| Skill 方法 | 登记能力目标、前置条件、工具依赖、参数获取、检查、恢复与重试上限 | 理解一种方法的适用条件 | 为具体执行方法提供可读契约；登记不等于已安装或已执行 |
| Tool 能力 | 登记真实接口、版本、输入输出结构、格式、运行环境及可用性证据 | 知道某工具能做什么、需要什么 | 按契约调用外部工具；知识库本身不启动这些 CAE 工具 |
| 验证基准 | 保存检查方法、预期结果、容差及来源、基准资产、检查程序 | 判断结果为何可以或不可以接受 | 制定验证步骤，比较实际结果与有依据的预期 |

一句话区分：**流程规则安排顺序，Skill 说明方法，Tool 提供实际接口，验证基准说明怎样检查。**

验证基准可以来自解析解、实验、可信参考算例等，但阈值、容差和适用条件必须有明确依据，不能因为历史案例通过就自动沿用到所有模型。

### CAE 工具与 Skill 从哪里来

当前收录 `D:\CAE-AGENT-DSH` 中两项 CAE Skill 及 `cae_viewer` 的 68 项 Tool 契约快照，包含来源路径、行号和 SHA-256，存于 `src/static/cae-catalog.json`。

这是从真实源码提取的能力目录，不是已经连接并验证所有工具可执行。外部工程更新后应重新核对目录；其他电脑也不会自动拥有这些本机路径或依赖。

### Jev 是否在 EXE 中运行

当前 EXE 的生产检索、问答和 CAE 执行不以 Jev 作为运行依赖。开发阶段使用 Jev 做分类、路由或审查建议，与把 Jev 集成到软件运行流程是两件事。不要因开发过程提到 Jev，就认为点击问答会自动调用 Jev。

## 10. 运行与验证：记录真实过程与缺口

| 类别 | 保存什么 | 对人的帮助 | 对 Agent 的帮助 |
| --- | --- | --- | --- |
| 任务定义 | 分析目标、模型版本、载荷依据、边界条件、资源、交付物、验收要求 | 开始前明确到底要完成什么 | 防止目标漂移，建立需要补齐的输入清单 |
| 运行记录 | 当前步骤、真实工具调用与返回、输入/日志/结果/截图、自动检查、人工复核、选用理由与失败恢复 | 回看执行证据，而非只看“成功”标签 | 支持续做和诊断，明确已完成和未完成步骤 |
| 知识与能力缺口 | 受阻阶段、缺口类别、缺少什么、已检索范围、补齐后验收方法 | 看懂任务为什么卡住 | 区分资料缺失、条件不明和检索故障，避免无依据补值 |
| 训练 / 评测登记 | 样本来源或模型家族、训练/评测划分、运行记录引用、筛选和纠错依据、复核记录 | 管理样本来源及质量 | 准备可追溯评测样本，减少来源混用；不是自动训练模型的按钮 |

记录需要由真实执行过程提供。知识库不会因为存在某个模板就自动生成真实求解日志、签名或人工复核结论。

## 11. 高级查看：原文片段与索引

日常查资料不需要进入这里。它主要用于解释“为什么搜不到”“命中是否来自完整正文”“哪些内容已经编码”。

### 搜索、阅读和编码单元

- 可按正文、文件名、标题、标签、分类、文件、内容质量、编码状态筛选，必要时使用对象 ID 或文件 ID。
- 查看完整切片、来源页码、所属对象、图谱入口、同文件前后片段及原文入口。
- 展开实际编码单元，查看进入向量模型的文本、字符范围、token 数、重叠和哈希。
- “原文片段”和“实际编码单元”不是同一计数单位；不同代索引的数量不能直接相减作为漏处理量。
- 子片字符位置相对父片，不等于原文件字节位置；页码及旧数据顺序问题需要对照原件。

对人：能看到系统实际用了哪些文字，而不只是一个进度百分比。对 Agent：能定位长文本截断、噪声、过期向量或来源定位问题。

### 内容质量标记

可以检测当前文件疑似噪声，也可以填写理由后确认噪声、恢复正文、标为待核查或撤销人工标记。

质量标记不改写原文、不删除原件，也不等于人工工程审核。疑似噪声不应不加复核地批量认定。没有明确失败记录的待编码单元，也不能标为“编码失败”。

### 索引管理

高级区提供“新索引全量 / 续建”“暂停新索引”“校验并切换新索引”。构建完成后仍须通过校验和切换，才能成为实际检索所用索引。

旧索引、新索引进度和原件应保留。不要为了让数字看起来一致而删除旧索引，也不要把“新索引就绪”说成“当前查询已经使用新索引”。本次文档发布不执行索引切换。

## 12. 高级查看：任务知识包

任务知识包是一次具体仿真任务的“固定版本依据清单”。

使用步骤：

1. 填写任务包名称。
2. 选择相关任务、模型、材料、工况、网格策略、流程、Skill、Tool 和验证基准记录。
3. 点击“检查缺口并导出”。
4. 阅读缺口清单，并将导出的 JSON 交给后续任务或 Agent。

当前界面从有限的候选列表选择对象，单包最多选择 100 个对象。缺口检查包括必要类型是否存在、来源/适用范围/单位是否填写、对象是否已复核；这不是对全部工程条件的完备验证。

快照包含选定对象及版本、文件标识与校验值、包内对象之间的关系、主库版本及缺口。导出的是 JSON，不会把所有原始文件、软件依赖和求解器一起复制成可执行工程。

对人：便于交接和复盘。对 Agent：限制上下文范围，知道采用了哪个版本，避免从整个库随意拼凑输入。

当前任务包明确返回 `executable: false`、`agent_connected: false`：没有因此授予执行权限，也没有完成全部适用性审核。零项形式缺口也不自动改变这一点。旧快照不会自动变成新版本任务包。

## 13. 共用功能：详情、编辑、原件与版本

打开对象后，可以阅读完整字段、查看保留的结构化原始记录、编辑记录、导出完整 JSON、附加原件、查看历史版本并下载某个版本。

附加原件会将文件复制到知识空间并登记 SHA-256。文件登记不等于全文已成功解析、内容已正确抽取或工程适用性已验证。

JSON 导出适合程序读取与交接；它不是全库备份。历史版本用于理解变更，不应静默覆盖已有任务依据。

常见状态：

- 候选：尚待复核，不作为已批准知识。
- 已复核：记录处于复核状态，具体复核范围仍需看证据；不等于适航批准。
- 冲突：存在需要处理的矛盾，不应忽略后继续选用。
- 停用：不宜继续作为当前依据；保留历史追溯价值。

## 14. 后台自动处理：用户不必操作的加工流程

授权 Agent 提交明确指定的文件后，后台保留原件、按哈希去重、解析正文，再调度知识抽取、来源校验、候选登记及索引维护。它不会因此获得全盘扫描权。

| 后台状态 | 含义 | 应如何理解 |
| --- | --- | --- |
| pending | 已排队 | 不是已处理完成 |
| waiting_model | 等待知识抽取模型配置 | 原件可已保存，但知识抽取尚未完成 |
| processing | 正在处理 | 需等待真实结果，不提前承诺成功 |
| needs_attention | 需要排查异常或歧义 | 交由管理 Agent 或人核对，不无限重试 |
| complete | 此处理任务完成 | 不等于全库索引已切换，更不等于人工工程批准 |

抽取沿用知识运行时配置，例如 `ZH_PIPELINE_MODEL` 及本机模型服务；工作台填写远端问答 Key，不会自动授权把整份原文送往远端抽取。

主界面已不单独显示“知识加工与问答”或“导入与质量”导航。后台协议、模板及历史数据仍保留；隐藏管理入口不等于删除处理能力，也不应把所有异常隐藏成成功。

### 增量更新和断点恢复

对象、文件、正文和关系变化进入与主库事务一致的事件队列。回答及其引用绑定来源版本，变更后可以识别依赖失效。

向量更新使用候选副本，复用未变化单元，保留当前索引和处理断点；校验通过后发布索引指针和发布清单。处理中源数据再次变化会重新排队，而不是无条件发布旧结果。

当前仍可能扫描源记录、重建图谱，不是每个阶段都只处理差异。SQLite 与外部文件也不是一个统一的分布式原子事务；安全性依靠版本、出处校验和拒绝陈旧结果，不能宣传为任何故障下都绝对无损。

## 15. Agent 如何接入，以及权限边界

在模型设置的折叠区，由本机所有者生成只读或资料处理凭据。Agent 使用 `Authorization: Bearer <token>`，不复制桌面所有者会话。凭据可撤销，程序关闭后失效。

当前服务地址从正式程序的 `data/runtime.json` 获取，端口可能变化。只在本机 loopback 使用，不把服务直接暴露公网。

| 身份 | 当前主要能力 | 不应推断的权限 |
| --- | --- | --- |
| 本机所有者 | 配置模型、设置外发许可、管理记录与授权 | 不代表自动具有资料版权或工程审批资格 |
| reader | 允许的读取接口，以及检索和问答 | 不能修改模型设置、外发规则或人工审批 |
| processor | reader 能力，加显式资料提交和重新处理 | 不能任意写对象、删库、创建任务包或修改审批 |

这是单机角色边界，不是按资料、项目、部门隔离的多人权限体系。读取权限范围较宽，不能把 reader 理解为“只能看某个指定文件”。它也不隔离同一 Windows 账户下的恶意程序。

常用接口说明（均需匹配授权）：

| 接口 | 作用 |
| --- | --- |
| `POST /api/retrieve` | 取得检索证据 |
| `POST /api/workbench/ask` | 使用工作台问答流程 |
| `POST /api/assistant/ingest` | 以 multipart 的 `file` 字段提交明确授权文件 |
| `POST /api/assistant/files/{file_id}/process` | 将已登记文件加入处理流程 |
| `GET /api/assistant/jobs` | 查看处理任务进展 |
| `GET /api/packages` | 读取已保存的任务包快照 |

角色许可仍受具体接口的校验和资料外发规则约束。不要绕过权限检查给 Agent 直接写数据库，也不要把 API Token、Key 或运行时会话文件提交到 GitHub。

## 16. 如何帮助 Agent 完成一次智能仿真

以“给定某部件模型，开展静力分析”为例：

1. **明确任务**：记录目标、模型版本、交付物和验收要求；缺载荷或边界条件时先登记缺口。
2. **找依据**：检索相关原文、材料卡、工况卡、网格策略、案例和验证基准，对照引文确认适用性。
3. **准备任务包**：固定使用的对象及版本，列出未复核项和缺少条件，而不是自动填满字段。
4. **选择方法与工具**：读取流程、Skill 和 Tool 契约，在外部 CAE 环境核查接口和依赖是否真的可用。
5. **执行仿真**：由外部 Agent 操作几何、网格、属性、载荷、求解和后处理；知识库本身不替代求解器。
6. **保留证据**：将真实输入、工具返回、日志、结果和截图登记为运行记录，再依据基准开展检查。
7. **处理失败**：区分工具故障、知识缺口和物理设置问题，记录已失败尝试；必要时停止并请人确认。
8. **形成可复用经验**：整理案例或候选知识，经过相应复核再供后续任务参考，不伪造审核结论。

知识库对智能体最重要的帮助，是让“为什么这样做、用了什么依据、当前还缺什么”可追溯，而不是让任何输入都无条件自动跑完。

## 17. 导入、备份与 GitHub 的区别

日常不要求使用者导入空白模板。资料处理由授权后台流程承担；对象详情仍保留附加原件等维护入口。

后端保留导入与备份能力，但当前不是一个日常导航模块。现有备份接口主要打包 SQLite 主库、管理的 assets 原件及清单，不是包含模型权重、全部运行时和所有向量索引的整机备份。需要迁移时应另行核对完整资产清单，不能只拿到一个 ZIP 就删除原环境。

GitHub 这次更新保存源码和文档，不备份本机知识库内容。业务数据、原件、索引进度和 EXE 应在本地按各自用途保留。

## 18. 常见问题与实际限制

| 问题 | 解释 / 建议 |
| --- | --- |
| 为什么有原文件却搜不到正文？ | 可能未解析、提取失败、扫描件无文本或索引未完成；先打开原件，再看后台和片段状态 |
| 为什么“只检索”成功，“检索并回答”失败？ | 还需检查模型连接、思考参数兼容性、远端及逐文件许可、引文支持情况 |
| 为什么相关资料没有形成回答？ | 相关不等于能支持结论；标题命中、无有效引文或来源变更都不能强行回答 |
| 为什么新索引已经就绪，却还是旧索引？ | 就绪是构建状态，实际使用还需通过校验并切换 |
| 为什么 Tool 已登记却不能运行？ | 目录只是契约；外部工具服务、运行环境、版本和执行授权需独立确认 |
| 任务知识包为什么不能直接执行？ | 目前明确不授予执行资格；它提供固定依据和缺口，不是可执行 CAE 工程 |
| 为什么旧引用需要重新检查？ | 文件、对象或关系版本变化后，历史引用不应自动被视为仍然有效 |
| 是否支持多人项目权限？ | 当前是本机所有者和有限 Agent 角色，不是企业多人账户与项目 ACL |
| 是否能自动完成工程审批？ | 不能。模型判断、自动校验、关系登记和求解成功均不能代替正式审核或批准 |
| 是否已验证我的远端模型可用？ | 文档和自动测试不能证明用户服务连通；须实际配置并测试，再核查真实问答 |

## 19. 实现依据与进一步阅读

本文依据以下文件核对；后续改版应同步更新说明：

- [桌面构建与运行说明](desktop-framework/README.md)
- [工作台与后台处理](desktop-framework/工作台与后台处理.md)
- [可信工作台 1.6 交付记录](desktop-framework/可信工作台1.6交付记录.md)（历史构建与验收记录，不代表当前模型连接或索引状态）
- [导航、对象详情与任务包界面](desktop-framework/src/static/app.js)
- [工作台界面](desktop-framework/src/static/workbench.js)与[工作台后端](desktop-framework/src/app/workbench.py)
- [结构化对象模板](desktop-framework/src/app/templates.py)
- [权限实现](desktop-framework/src/app/access.py)
- [原文片段与索引界面](desktop-framework/src/static/slice-workspace.js)
- [知识图谱界面](desktop-framework/src/static/graph-workspace.js)
- [CAE Skill 与 Tool 契约目录](desktop-framework/src/static/cae-catalog.json)

本文不是新的功能交付，也不意味着重新打包、重新建立索引或重新完成真实求解验收；它解释“成品1”已有能力、使用方式和边界。
