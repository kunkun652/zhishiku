# 知衡仿真知识库框架 · 交付与验收

交付日期：2026-09-19。

## 打开程序

- 专用快捷方式：`D:\zhishiku\新版知衡知识库.lnk`
- 实际EXE：`D:\zhishiku\desktop-framework\release\知衡仿真知识库\知衡仿真知识库.exe`
- 使用说明：同程序目录的 README.md。
- 空白模板：同程序目录 templates，共16种。
- 源码：`D:\zhishiku\desktop-framework\src`。
- 机器接口：同程序目录 openapi.json 与 接口与架构映射.md。

EXE SHA-256：`826E85285371109005EF028542659A85CDC9E97E4104F5D75589E374AEFB139F`。

这是便携目录版，必须保留 _internal、semantic-runtime、data 等配套目录；程序与依赖约610 MiB，不需要另启Python或DSH。首次运行使用本机已安装的WebView2，当前机器已实际打开成功。

## 原库与数据边界

原始启动脚本指向 `D:\知识库\work\rag-platform\dist_simcases_20260918\CAE知识库平台.exe`，该文件已复制到 baseline 并校验一致。原始EXE SHA-256：`0588BE95773AB40173A98387DACDC4B1878F7774D90EF60088A7F68F84773E60`。

复制范围内33个源码/前端资源逐一对比，未发现原文件改变。没有向D:\知识库写入代码、配置或数据。没有修改、启动或连接D:\CAE-AGENT-DSH。此前D:\zhishiku的旧演示及其资料保持原状。

正式程序的健康检查确认：业务对象0、数据修订0、Agent未连接。没有把旧数据库、原始资料、切片、模型或测试记录放进正式空间。原件与资料后续通过模板和受管附件逐步填充；尚未进行原库批量迁移。

## 已验证

| 检查 | 结果 | 证据 |
|---|---|---|
| 13组源码行为回归 | 全部通过 | evidence/tests.log |
| 16种模板读写与空值 | 全部通过，未知值保留 | 同上 |
| 中文、别名、条件过滤、无命中 | 通过 | 同上 |
| 版本并发冲突、历史快照 | 通过，旧版本不被覆盖 | 同上 |
| 非法材料卡、伪造复核状态 | 拒绝或降为候选 | 同上 |
| 图谱有限跳数与版本失效 | 通过 | 同上 |
| 文件校验、防越界、去重 | 通过 | 同上 |
| 备份与新空间恢复 | 通过 | 同上 |
| 打包后14项真实API检查 | 全部通过 | evidence/packaged-api-checks.json |
| 打包BDF/STL解析 | 三角形测试夹具实际解析通过 | 同上 |
| Semantica独立EXE | 3对象/1关系构建通过 | evidence/final-semantic-check.json |
| Semantica数据更新失效与重建 | stale检测通过，重建后恢复 | 同上 |
| 进程重启持久化 | 测试对象和版本保留 | 同上 |
| 最终EXE空库启动 | 0业务对象，Semantica空图重建通过 | evidence/delivery-check.json |
| 原库保持不变 | 原EXE哈希一致，33文件未变 | evidence/original-preservation.json |

浏览器操作实际验证了：模板表单新建并保存术语、英文检索命中完整中文卡片、模型记录与附件显示、BDF真实三角形/线框显示、图谱节点关系与Semantica按钮。页面截图排版可读，检查的浏览器错误/警告列表为空。

使用Windows窗口工具实际定位打包EXE并查看桌面首页截图，页面正常显示。首次窗口被遮挡，重新选择并前置后截图成功；没有把遮挡截图当作验收。以上为自动检查与工具视觉检查，不代替用户视觉验收或人员工程复核。

所有合成验收数据仅保存在 evidence/runtime-test-data 和 evidence/desktop-window-test 等测试空间；正式data保持空库。

## 尚未实施的能力

- 真实语料迁移、文档切片、OCR、embedding、LLM问答。
- DSH/PI实际接入、求解任务编排和执行控制器。
- CAD语义区域自动测量/重新绑定、实体单元网格显示、OP2结果场解析。STEP/IGES/BREP沿用原查看器，本轮未用用户模型逐格式验收。
- 完整工程适用性规则、正式人员复核晋级、工程/适航批准。任务包当前一律不可执行。
- 自动训练样本筛选与冻结、独立评测、微调训练。
- 多用户鉴权、正式安装器、代码签名、大规模语料性能验收。

这些边界已在程序说明和接口文件中列出，未使用假资料或假成功状态填补。

最终窗口工具限制：最终版本已按实际EXE路径启动，PID和健康检查见delivery-check.json。最后尝试将窗口置顶时，窗口工具两次返回 failed to activate captured window，因此最终置顶状态未确认；此前版本相同UI的桌面截图与本轮浏览器交互检查已通过。
