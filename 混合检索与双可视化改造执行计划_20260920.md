# 知衡混合检索、Wiki 关系与双可视化改造执行计划

本文可完整复制到新的 Codex 任务执行。编制日期：2026-09-20。当前任务仅调查和编写计划，未改动应用、数据库或 EXE。

## 给执行任务的指令

请在现有知衡仿真知识库中落实以下改造，完成实现、必要测试、真实数据验证、打包及正式目标验证，不要仅再次提交设计建议。先核对现场状态，按阶段实施，保留用户已有数据、改动和工程审核边界。普通可逆实现选择自行决定；真正影响数据安全、外部发送或缺少必要信息时再提出具体问题。不要把多路检索理解为必须启动多个开发代理。

### 1. 工作目录和现场基线

- 当前项目：`D:\zhishiku\desktop-framework`。
- 正式 EXE：`D:\zhishiku\desktop-framework\release\知衡仿真知识库\知衡仿真知识库.exe`。
- 快捷方式：`D:\zhishiku\新版知衡知识库.lnk`。
- 图谱交互参考：`D:\知识库\work\rag-platform\static\knowledge-graph.js`。
- 参考后端：`D:\知识库\work\rag-platform\app\explore.py`、`app\rag.py`。
- 本地 Semantica 源码：`D:\zhishiku\semantica`。
- 当前实现优先阅读：`src\app\main.py`、`src\app\retrieval.py`、`src\app\semantic_search.py`、`src\embedding_worker.py`、`src\semantic_worker.py`、`src\static\app.js`，以及现有打包脚本、测试和交付报告。
- 参考目录只读，不迁移旧数据库，不修改其他 CAE 工程。读取实际存在的 AGENTS.md；2026-09-20 检查时 `D:\知识库\AGENTS.md` 不存在，不要把历史记忆当现存规则。

以下是编制时的只读核查结果，执行时重新确认，不能当作永远不变的常量：

1. 当前主库有 6,978 个对象、2,391 条关系；1,650 个对象至少有一条边，5,328 个对象没有边。其中孤立对象包含 3,618 个资料对象、1,659 个模型对象、50 个任务和 1 个数据集。
2. 当前 `/api/graph` 最多选择 200 个节点，再过滤掉端点不在所选节点内的边。因此画面不完整既有真实缺关系，也有显示截断，不能混为一个问题。
3. 当前图谱渲染重新创建 SVG 和节点布局，画布高度固定 470，没有完整的视角持久化和初始全范围适配。
4. 旧项目具备右侧详情、来源、邻居高亮、底部图例、一次自动适配及交互后取消自动适配，可借鉴；它重新加载图谱时也会重建 SVG，不应照搬这个缺点。旧项目的边来自登记字段的明确引用，不是自动工程推理。
5. 已有关键词、本地 BGE-M3 向量和图谱关联展开。当前图谱关联主要是一跳扩展，不等于完整的生成式 GraphRAG。
6. 当前 Semantica 使用 GraphBuilder 0.6.8，派生状态记录为 6,978 节点、2,391 边。下载源码版本不代表打包运行版本已经升级。
7. 原索引目标 555,413 个检索单元由 6,978 个对象和 548,435 个文本块组成，不能都称为切片。实际完成量必须现场读取。
8. 当前编码采用最大 512 tokens 的输入，既有长文本块可能只编码前部。512 是当前编码输入上限，不是已经保证每片完整覆盖的分片长度，也不是模型能力的总上限。

### 2. 最终架构与明确分工

实现“一份权威主库、两个可视化工作台、三条召回通路、一个检索编排器”。

| 层/组件 | 责任 | 不承担的责任 |
|---|---|---|
| 权威主库 | 原文件引用、完整知识卡、实体 ID、版本、明确关系、关系依据及状态 | 不被索引或派生结果覆盖 |
| 关键词检索 | 编号、材料牌号、术语、原文词句和精确字段匹配 | 不凭词语相似证明工程适用 |
| 向量检索 | 对问题和文本检索单元编码，发现不同说法下的相关资料 | 不产生事实关系，不改写完整卡片 |
| 图谱检索 | 找实体，按有依据的关系和方向遍历，找关联对象及原文 | 不把任意邻居都当作有效答案 |
| GraphRAG | 将图谱检索得到的路径、实体和原文组织为回答上下文；有问答模型时生成带引用回答 | 不等于一张可视化关系图，也不要求安装微软全套实现 |
| Semantica | 可替换的图谱构建/处理后端，形成可追溯派生图；额外能力必须真实实现并验证 | 不算第四路重复召回，不成为第二份工程事实源 |
| 编排器 | 按问题选择通路，控制并发/预算，融合去重、排序、输出证据和运行说明 | 不默认所有问题跑遍所有组件 |

Semantica 决策：保留现有接入并建立薄适配层，不为使用它而重复构建一整套应用。图谱通路提供主库后端与 Semantica 后端的相同查询契约；同一请求通常只使用一个后端，不能把同一条关系返回两次再提高得分。以一致性、查询能力、耗时和维护成本比较两者。如果 Semantica 目前仅复制已有关系，就明确其价值暂为派生构建/校验，不宣称检索更智能。其不可用时仍能直接查主库。未来仅在真实需要并验证抽取、图算法等能力时扩展，不预先引入重型服务。

### 3. 双可视化入口

只做两个主要入口：

1. “切片与索引”：看资料如何分片、哪些片已编码、实际覆盖和搜索命中。
2. 升级现有“知识图谱”为图谱工作台：同一画布切换“主库关系 / Semantica 派生 / 一致性差异”。默认主库关系，Semantica 是可检查的派生视图；不再增加第三套独立图谱页面。

图谱节点的详情可跳到原文切片，切片详情可跳回所属知识对象和关联图谱。两种视图互相定位，但保持各自的筛选与视角。

### 4. 切片与索引改造

4.1 先清点文档、页面、已有文本块、对象检索表示、失败解析和索引状态。分别显示数量和统计口径，不能把“有向量”当成“整份原文已覆盖”。扫描 PDF 无正文时显示需要 OCR，不伪报完成。

4.2 使用实际模型 tokenizer 分片和计数。第一轮可沿用 512 输入预算试验，但正文、标题前缀、特殊 token 的总和必须在预算内。优先段落/标题边界，长段再拆分，重叠可从约 64 tokens 试验，经质量和速度基准确定。不得静默截断；公式、表格应保留语义结构和原文定位，无法可靠解析时标记。

4.3 每个新检索文本单元记录：稳定 ID、父对象/父片 ID、文件 ID 与哈希、页码或页码范围、可用的原文字符位置、正文、token 数、分片规则版本、内容哈希、模型及 tokenizer 版本、编码状态和失败原因。原文位置无法可靠获得时明确缺失，不能编造。重叠内容可回溯，不因页间拼接伪造单页出处。

4.4 知识卡的完整结构保持不变。可以生成标题摘要、字段组合等检索表示，必要时分多个关联表示，但它们永远指回完整卡片，不能把权威卡片替换成碎片。

4.5 UI 使用文件/对象筛选、分页切片列表、完整片段详情与原文定位。显示本片字数、tokens、实际送入编码的文本、索引版本及状态；支持上一片/下一片、查看重叠区、查看所属对象、打开原文。查询结果能够直接定位到命中片段。

4.6 原索引继续可用，在新版本目录或新表建立修正后的索引。模型/分片签名不同的向量不能混用；试点验证后才全量建立，完成一致性检查后原子切换。旧索引保留可回退，不能先删除正在工作的索引。支持中断续建、变更重算、失效清理和进度持久化。

4.7 后台编码让位于前台检索；控制批量与 GPU 使用，避免每写一批就重新加载整个向量矩阵。先测当前全扫描的延迟、内存和索引规模，再选择增量缓存或本地 ANN 索引，不默认另部署向量数据库。使用 ANN 时检验过滤后召回不足和召回率损失。

### 5. 主库的 Wiki 化及关系建设

5.1 每个对象拥有统一详情页：名称、别名、类型、正文/字段、适用范围、版本、来源、出链、反向链接、关系依据及待处理建议。支持明确的对象链接；标题重名时选择对象，存稳定 ID，不通过相同文件名自动合并。

5.2 关系包含稳定 ID、源/目标 ID、类型、方向、依据原文和定位、源文件哈希、生成方式、端点版本、状态。自动方法可记录规则或模型版本；没有校准依据时不展示虚构置信度。沿用既有工程审核机制，关系已登记不等于参数获批。

5.3 分开管理三种来源：
- 已有结构化字段中可验证的明确引用：自动补齐“引用/来源于”等对应关系，并记录具体字段依据。
- 文本中的明确关系陈述：抽取为候选，给出原句、位置和目标对象，支持批量接受/拒绝及撤销。
- 共现或向量相似：只显示“可能相关”的建议；不能直接变成“采用此材料/适用于此工况”等事实关系。

5.4 增加孤立节点诊断：真实没有登记关系、显示边界外、无效引用、目标缺失、候选待处理分别统计。优先根据明确引用补链，输出改善前后数量和来源；不以让所有点连接为目标，不生成装饰性连线。

5.5 用户点击“寻找关联”后给出有依据的候选和解释。没有配置可用抽取模型时，明确启用规则/显式引用能力，不假装已完成大模型抽取；不得默认将本地资料上传外部服务。接受、拒绝、撤销和源文件更新后的过期标记必须可追溯。

### 6. 图谱工作台交互

6.1 参考旧项目的右侧详情、底部图例、邻居高亮、来源点击和一次适配。升级为清晰的节点下方标签、选中/悬停时显示完整名称、类型颜色和关系状态图例。大量节点时按聚合/局部展开减少标签重叠，不能把字号无限缩小。

6.2 首次加载适配一次；点击节点只更新右侧详情，不重建画布、不重新居中、不改变缩放。独立提供“锁定布局”“锁定视角”“适配当前内容”“重置视图”。锁定布局停止力布局漂移；锁定视角禁止自动平移/缩放。拖动节点与点击通过距离阈值区分。

6.3 保存 zoom transform、节点位置、选中节点和筛选状态；切换详情、异步加载和返回页面时恢复。窗口尺寸变化保持观察中心，重新加载数据尽量沿用存活节点坐标。明确切换数据集时才允许用户选择重新适配。

6.4 修复 200 节点隐藏截断。总览使用聚合图，局部支持一跳/两跳、按关系类型展开和逐批加载；搜索可定位主库任意对象。显示“全库多少/当前显示多少/范围外还有多少关系”，边界有继续展开入口。不得把有限样本称为全图，也不必将七千节点都塞进同一张力导向 SVG。

6.5 右侧详情显示完整内容、原文件、关系列表、依据、版本状态、反向链接；支持手工建立有依据的关系和查看候选。保留视角的前提下刷新局部边。定位名称使用专用对象查询，有歧义显示候选，不能取混合搜索第一名直接当目标。

### 7. Semantica 派生一致性

7.1 主库为唯一编辑入口；派生图从一致的数据库快照构建。派生文件和 manifest 在临时位置完成后原子发布。构建期间主库变更时，该次结果不能标为当前一致。

7.2 manifest 至少包含源库 revision、构建版本、Semantica 实际版本、schema 版本、时间、节点/边数量和规范化内容哈希。

7.3 校验节点/边 ID 集合、类型、方向、端点版本、依据和权威字段映射，展示缺失/额外/变更/悬空引用差异。仅数量相等或 revision 相等不够。若派生图有额外推断节点/边，单独命名空间及图层，不能把它们纳入权威等价声明。

7.4 UI 标记“一致、主库已更新、构建中、失败”，差异可定位对象。过期结果只能作为标注清楚的历史视图；实际检索回退到主库并显示后端与原因。构建失败保留上一份可用产物，不显示假的零节点成功状态。

### 8. 多路检索与 Local GraphRAG

8.1 用户模式提供“自动、关键词、语义向量、图谱关系、混合”；高级选项可配置参与通路。Semantica 后端选择放在图谱设置中，不作为与图谱重复的召回开关。

8.2 自动路由先做可解释规则，必要时才使用已配置的模型：
- 标准号/牌号/对象 ID：精确字段和关键词优先，严格保留用户指定约束。
- 自然语言描述：关键词与向量并行召回，再融合。
- “某案例用了什么材料/关联哪些报告”：先消歧定位对象，再按关系类型和方向查图，并返回原文。
- 对比多个案例：分别召回候选，再扩展所需关系。
- 全库主题总结：当前优先返回主题资料与证据；社区摘要/Global GraphRAG 作为后续明确需求阶段，不默认运行昂贵的全库总结。

8.3 图谱通路优先一至两跳，限制种子数、关系类型、方向、分支和总证据预算。路径有完整端点版本和每条边的依据，防止循环和高连接度通用节点淹没有效结果。可扩展到三跳须有基准支持，不默认无限遍历。

8.4 各路使用统一的对象类型、分类、版本、状态和适用性规则；能前置的过滤前置，不能前置时过量召回和补足并报告不足。未知适用性明确标记待确认，不当作符合；明确冲突不得以相关资料的身份冒充适用资料。

8.5 融合从 RRF 等可解释方法起步，按对象聚合并保留不同命中片段、关系路径和来源。不直接相加余弦分数与关键词分数。可选本地 reranker 需经评测证明收益，不能为堆模型牺牲交互。相同边由主库和派生图返回不能获得双倍权重。

8.6 统一结果包含：对象 ID/版本、命中片段 ID、召回渠道、图谱后端、原文件/页码/可用偏移、原文摘录、关系路径、适用性状态、各路排名、索引与主库版本。技术指标默认折叠。

8.7 页面显示易懂标签：“关键词命中”“语义相近”“关系路径发现”；支持展开“为什么找到它”。检索运行摘要显示所用通路、覆盖范围、降级和耗时。相似度不是工程正确率。

8.8 给各路设超时和取消；用户发起新搜索取消旧请求。部分通路失败时返回可用结果并清楚标记。缓存按查询、过滤条件及索引/图谱版本隔离，分页固定检索快照，避免后台建索引造成翻页重复或漏项。

8.9 未配置问答模型时，交付的是图谱增强检索与证据展示，不声称已完成生成式问答。配置模型后增加“基于资料回答”：逐项引用能打开的原文，关系性结论引用完整路径，证据不足明确说明。检索能力不得依赖问答模型可用。

### 9. 分阶段执行与退出条件

阶段 A：只读核对现场、记录正式 EXE/运行端口/数据位置/哈希和索引进度；建立一致性备份和小型代表性评测集。输出差距清单及实施文件范围。

阶段 B：先修图谱截断说明、首次适配、点击详情保持视角、底部图例和局部展开。此阶段不等待全量向量重建。验证真实孤立与显示遗漏可区分。

阶段 C：确定最小数据迁移、来源定位和版本契约；完成 Semantica 快照、差异校验、主库回退与图层切换。旧数据和旧读接口继续可用。

阶段 D：完成切片查看器、token 预算修正和新索引试点；通过尾部内容、跨页定位、完整卡片与断点续建验证后再全量运行。全量过程中继续可用旧索引，状态不得误标全部完成。

阶段 E：实现 Wiki 详情、双向链接、显式引用补链、候选关系审核/撤销和孤立节点诊断；修改关系后能局部更新视图并使派生状态变旧。

阶段 F：实现统一检索契约、自动/手动模式、并发召回、图谱路径、融合和性能优化；对有模型环境补充有引用的回答验收。以基准数据判断是否启用 ANN/reranker，不先堆依赖。

阶段 G：回归、真实数据 API 与界面验证、构建独立产物、备份后更新正式目标、验证实际运行版本。确认存在未保存内容风险时再处理关闭窗口问题，不能未经判断强杀。最终给出目标路径、哈希、截图、评测及剩余限制。

每阶段完成可运行的最小闭环，记录进展，不重写无关模块。全量任务耗时较长时保存任务状态、日志与预计剩余工作，不将“任务已开始”写成“全部完成”。

### 10. 必须通过的验收

功能与正确性：
- 主库完整对象、原文和既有关系保留；迁移与索引切换可回退。
- 超过 512 tokens 文本的尾部事实可检索到对应新片，实际编码文本无静默截断；页码与原文一致。
- 卡片仍完整，检索表示可回到原卡；失效/删除内容不继续作为当前结果出现。
- 任意对象能定位，截断被明确告知，外部邻居可继续展开。
- 点击不同节点、开关详情、异步完成和页面返回不会自动放大或平移；锁定布局时节点不漂移。记录变换矩阵及界面操作证据。
- 自动补链有明确字段或原文依据；相似建议不会变成已确认工程关系；拒绝/撤销有效。
- 主库更新立即使旧派生状态失效；缺边、多边、端点版本和依据变化都能被校验识别；失败后主库检索继续可用。
- 关键词、向量、图谱和混合模式可单独验证；结果标签反映真实调用通路，不能按选择框名称伪造。
- 已知精确编号测试正确命中；明确不适用条件不会被伪装成适用；每个展示的引用能够追溯。

质量与性能：
- 建立约 60—100 条代表性问题，覆盖精确编号、同义改写、长文尾部、中文/英文、关系一跳/两跳、组合筛选、无答案和版本冲突。保留独立测试集，避免同一材料的近似改写占满样本。
- 用可核查的相关文件/片段和关系路径作为答案依据；需工程判断的标签留给人工确认，不由模型自评制造满分。
- 对比关键词、向量、图谱、混合的 Recall@20、nDCG/MRR、引用可追溯率、关系路径正确性；同时比较主库与 Semantica 后端的一致性。
- 测冷/热启动、后台建索引开/关的 p50/p95 查询耗时、内存/显存、建索引速度、磁盘占用和图谱交互。先记录现状并设可实现目标，不预写“毫秒级”或虚构提升百分比。
- 必须检验所改问题的改善和其他模式无明显退化；收益不足的复杂组件保持关闭并解释。

交付物：代码与迁移、必要回归测试、双可视化界面、检索基准及结果、Semantica 保留/启用决策记录、实际目标 EXE 及哈希、备份/回退说明、普通用户使用说明。区分源码测试、独立构建测试、正式运行验证和用户视觉验收，不能用报告代替结果。

### 11. 参考资料与概念边界

- Microsoft GraphRAG 的 Local Search 结合实体关系和原文；Global Search 使用社区报告处理全局问题。这里先落实面向本库的局部图谱增强检索，不把它冒称已完整移植 Microsoft GraphRAG。参考：https://microsoft.github.io/graphrag/query/overview/
- Semantica 上游项目：https://github.com/semantica-agi/semantica 。执行前核对现用版本 API，不能把最新上游宣传当成本地已具备能力。
- 旧项目的本地源码作为交互和来源追溯参考；本计划检查了代码，未声称完成旧项目当前运行界面的视觉验收。
