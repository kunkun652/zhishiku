# 独立验收报告

## 当前结论（2026-09-18 17:10，覆盖下方历史阶段结论）

**本地样本演示功能有限通过；整体最终验收尚未通过。** 真实资料阅读、卡片、基础来源图谱、CAD/网格/结果展示、复制版DSH嵌入和CAE只读能力已可演示。DeepSeek没有配置，未完成真实模型问答、由模型发起工具调用及自动整理闭环；不能宣称“全部完成/没有问题”。

运行入口：http://127.0.0.1:8765/ ；智能助手：http://127.0.0.1:8765/#assistant 。启动为 `D:\zhishiku\启动知识库.ps1`。当前运行服务保留，源目录未由验收修改。

### 最后一轮独立检查

- 重跑 scripts/verify.py：49/49。此数字包含静态检查与实施侧记录读取，只作为辅助证据。
- 重新读取活动进程：知识API PID7736；其DSH PID22496；该DSH下知识MCP PID43948和Viewer MCP PID49640均存在，Python/Node可执行路径位于本项目。实际patch包含两种MCP；启动脚本按本项目CAE进程监听端口探测，当前动态API为127.0.0.1:61861。
- 新执行Viewer MCP探针，使用当前动态API，发现68工具、model_summary只读调用成功；未调用模型、未导入模型到原生客户端、未求解。该探针是独立协议验证，DSH模型编排链仍未验收。
- 直接调用当前知识工具：kb_search(query=材料,type=材料卡)返回17项且全部为材料卡；kb_preview_asset(mesh-wingbox)返回正确模型直达URL。浏览器实际打开后自动选中wingbox，显示2675节点/2657单元/4928三角形。PDF返回不支持三维预览并保留原件URL。
- 独立cua浏览器检查：内嵌原生DSH和CAE模式可见；frame STEP真实载入并可旋转；wingbox、framedeck显示；plate OP2显示真实位移颜色和0至3.7992e-5范围，单位未知标记保留；图谱可见；Pazy第1页详情含原文和页码原件链接。
- 11个登记资产源/副本哈希均重算一致；设计原件哈希回归通过；两份用户BDF原始卡片计数独立核对一致。证据位于 artifacts/independent-review-20260918.json，以及本任务实际工具记录。

### 对照原方案的10项门槛

| 门槛 | 当前判断 | 限定范围 |
|---|---|---|
| 1 原件保护与目录 | 通过抽检 | 两份设计原件及11个样本；未声称全盘内容逐字节审计 |
| 2 运行与统计 | 通过 | 130活动对象、11资产；仍依赖本机PowerShell及知识API所用bundled Python |
| 3 真实检索与来源 | 通过样本验证 | Pazy/网格/材料/模型查询；未启用向量检索 |
| 4 完整四类卡 | 通过读取验证 | 17材料、84术语、11网格、10工况；内容未获工程复核 |
| 5 图谱 | 有限通过 | SQLite派生的基础来源关系；Semantica仅GraphBuilder实测探针，未正式承担图谱服务 |
| 6 三维与结果 | 通过演示验证 | 2个CAD、3个FEM对象；仅plate有结果，frame与framedeck映射待确认 |
| 7 DSH/模型调用 | 部分通过 | 复制版原生嵌入、CAE预设、双MCP与只读探针通过；DeepSeek真实调用未通过 |
| 8 导入/源保护 | 有限通过 | 固定样本重复导入与版本回归；不是全库自动清洗、自动分类完成 |
| 9 任务包 | 通过当前候选场景 | 含版本/哈希和资产依赖；未知关键条件不可执行。未验证正式工程可执行包 |
| 10 浏览器验收 | 通过已列页面 | 主界面、来源、图谱、模型和DSH嵌入已亲自检查；小视口图谱可有水平滚动 |

### 尚未完成的能力与下一步

1. 用户需在内嵌DSH设置中配置DeepSeek模型凭据（在本机界面操作，不发到聊天）。随后再执行真实“提问→检索→读取来源→模型答复/预览工具”的闭环，并依据调用证据更新状态。现有model-status.json是验收状态记录，不能仅因填入凭据便当作已连通。
2. 当前全库只有路径级盘点，入库/解析限11个明确资产；大量PDF、案例、数据和日志尚未完成结构化。DeepSeek自动整理、候选关系写回和人工复核流程不在本次完成范围。
3. Semantica仅隔离环境运行探针，主图谱由SQLite关系提供。未完成正式Semantica集成、语义检索、求解与工程验证、EXE知识库独立安装器。
4. 这些公开/本地样本不能称商飞内部数据，原始结果不能当成本轮求解。wingbox/framedeck没有登记结果；frame.STEP和framedeck.bdf没有已核实的几何-网格映射。

自动跟进将暂停等待用户配置模型；不会重复运行同一套已通过检查或宣称整体通过。下方保留历史发现与修复过程。

---

## 第三轮独立复验（2026-09-18 16:46–16:50）

- 实际重跑 verify.py：41/41。该检查包含若干静态字符串及旧探针读取，故不能代替下面的独立检查。
- 自行请求 API、逐一重算11份源文件/副本SHA-256，全部相等。额外查询含连字符、引号、百分号和无命中词，没有再崩溃。独立原始BDF行计数与界面一致：wingbox GRID2675/CQUAD4 2464/CBUSH193；framedeck GRID318738/CTETRA889052。证据：artifacts/independent-review-20260918.json。
- 允许的 cua 浏览器工具已恢复可用；亲自看到 DSH 原生嵌入界面、CAE模式和工作区控件。frame STEP 实际加载 85420顶点/81088三角形，约7469ms；拖动后视角确实改变。wingbox与framedeck均真实可见，后者显示318738节点/889052单元/635318外表面三角形；无结果标记保留。
- **新发现：内嵌助手能力未接完整。** API托管的DSH PID10376只带知识patch；integrations/dsh/cae-kb.cordis.yml仅插入知识MCP，server/app.py不设置Viewer MCP需要的CAE_AGENT_API_URL等环境；该进程仅有一个Python MCP子进程。复制的CAE preset明确依赖宿主patch插入Viewer，不会自行插入。原生客户端的双MCP证据不能替代内嵌DSH的双MCP证据。
- 源码还显示kb_search声明type参数却未应用；kb_preview_asset给出的preview_url是原件下载地址，不是可打开所选资产的三维页面。要求实施侧修正这两个实际契约差异，并保持无模型凭据时的诚实状态。

仍未最终通过：先完成上述窄范围接入修复，再检查内嵌会话实际工具列表与只读调用；DeepSeek真实调用保留未通过。

## 第二轮复核进展（2026-09-18 15:55）

规划任务重新请求当前运行 API，确认：DPW-W1 查询返回 1 条结果；中文 card-term 原件返回 HTTP 200、234307 bytes；CAD+任意材料/工况返回 executable=false 并列出绑定缺口；plate 任务包包含 mesh-plate 和 result-plate。以上四项已独立复现修复，不等于其余项目验收通过。

用户在修复期间追加的两项现在交下一轮实施，未打断前一轮：

1. `D:\demo3\frame.STEP`（14930768 bytes）作为 CAD 演示，需真实 WASM 解析和来源核对；与 framedeck.bdf 同源/映射关系仍待证据。
2. 从 `D:\CAE-AGENT-DSH` 复制独立 DSH Web 和 CAE 模式及必需组件到本项目。当前启动脚本仍直接引用外部目录的 Node 和 DSH bin，尚不满足这项新要求。已发现源预设 `presets\cae\agent.cordis.yml`、`presets\cae\preset.yml`。需最小必需依赖复制、版本/哈希清单、私有状态隔离，保留原目录；不仅复制预设文件，还需验证模式发现与能力握手。

整体状态仍为未完成，等待补充实施及最终独立视觉/接口复验。

2026-09-18，首轮独立复核。结论：**未通过，已交回修正；不得把实施侧 15/15 当作整体通过。**

## 已核对

- 交接文件、实际 server/app.py、scripts/import_data.py、scripts/verify.py、app/app.js 和启动/停止脚本已读取。
- 实际 HTTP 首页能打开；浏览器显示 127 对象、8 资产，DeepSeek 未连接。
- 当前 SQLite 来源结构与接口存在；实现侧只导入 8 份样本，并非全库整理完成。

## 可复现缺陷

1. `GET /api/search?q=DPW-W1` 返回失败（本机请求表现为 HTTP 502）；后端实际日志为 `sqlite3.OperationalError: no such column: W1`。用户输入直接进入 FTS MATCH 语法，连字符、标点需按普通查询安全处理。
2. `GET /api/assets/card-term/file` 失败；日志为 Content-Disposition header 的 `UnicodeEncodeError: latin-1 ...`，中文原件名称不可直接写入该头。
3. 任意首张材料卡 + 首张工况卡 + `case-cad-dpw` POST `/api/package`，实际返回 `executable=true,gaps=[]`。CAD 浏览样本没有经过材料/工况/单位/载荷/模型绑定检查，不得因对象类型齐全而可执行。
4. 源码显示任务包只收集对象 source_asset_id，未收集案例 data.asset_ids，有限元案例可能漏掉 OP2 等引用；需要依赖完整性与版本哈希锁定。
5. 资料详情只展示 data 字段，PDF 正文 body 没有展示，也没有原件打开按钮。资产图谱节点也只显示状态，无法完成来源阅读链。
6. 导入脚本清空 objects/assets/edges，每个 version 恒为 1，源内容变化会静默覆盖相同 ID/版本，未实现方案的变化生成新版本。
7. makeScene 释放 renderer 却没有停止旧动画、释放 controls/geometry/material；切页也保留运行循环。大模型加入前需修复生命周期。
8. DSH 页只有文字说明，没有用户能打开的认证入口；根 URL 为 401。缺凭据不等于可以缺少可访问的隔离 DSH 配置入口。

## 用户追加演示模型

用户明确要求等首轮完成再加入 D 盘 wingbox 和 frame，不打断初始实施。现首轮已完成，开始补充：

- `D:\wingbox.bdf`，410603 bytes；另有 `D:\wingbox_stitched_together-000.bdf`，相同大小，需哈希核对去重。
- `D:\framedeck.bdf`，67977713 bytes，文件头为 HyperMesh 2026 MSC Nastran SOL101，含 PSOLID 和续行。

只读原件，先核对完整格式与解析支持。不得把有限元文件称 CAD，不能伪造求解结果。大型实体网格应正确提取外表面、保留节点/单元来源标识并报告实际数量；不允许通用扇形三角化实体单元掩盖错误。

## 后续

交同一 sol/medium 任务修正并补充两模型，保留服务可验收。之后继续独立检查 CAD、网格/结果、图谱、来源、版本、任务包和 DSH 接入。DeepSeek 凭据仍为待处理事项，尚未完成模型真实调用验收。
